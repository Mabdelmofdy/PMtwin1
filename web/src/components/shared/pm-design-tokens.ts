/**
 * PM-Twin design token helpers — backward-compatible re-exports.
 * @deprecated Import from `@/tokens` for new code. Layer authority: DDS-002.
 * CSS source of truth: web/src/index.css
 */
export {
  pmMotion,
  pmShadow,
  pmTypography,
  pmLayout,
  pmRadius,
  pmSurfaceTone,
  pmMatchTypeStyles,
  resolveMatchTypeStyle,
  formatMatchTypeLabel,
} from '@/tokens'
