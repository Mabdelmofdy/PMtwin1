/**
 * PM-Twin layout rhythm — backward-compatible re-exports.
 * @deprecated Import from `@/tokens` for new code. Layer authority: DDS-002.
 */
export {
  pmBreakpoints,
  pmBreakpointMedia,
  pmLayoutGrid,
  pmSticky,
  pmContentWidth,
  resolveMetricColumns,
  type PmBreakpoint,
} from '@/tokens'
