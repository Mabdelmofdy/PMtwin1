{
  "name": "@pm-twin/collaboration-models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical collaboration taxonomy — main models, sub-models, exchange modes, matching topology rules",
  "main": "./dist/index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "node esbuild.js",
    "test": "npm run build && node --test tests/**/*.test.js"
  },
  "dependencies": {
    "@pm-twin/party": "file:../party"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
