import type { MatchEngineInput, ModelRunResult } from '../types/engine.ts';
/**
 * Pure matching engine entry point — normalizes input, routes models, scores, ranks.
 * No I/O, persistence, or side effects.
 */
export declare function runMatchingForPost(input: MatchEngineInput): ModelRunResult[];
//# sourceMappingURL=run-matching-for-post.d.ts.map