{
  "name": "@pm-twin/decision-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Config-driven decision engine for approvals, delegation, escalation, and SLA governance",
  "main": "./dist/index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "index.d.ts",
    "src"
  ],
  "scripts": {
    "build": "node esbuild.js",
    "test": "npm run build && node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
