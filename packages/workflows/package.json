{
  "name": "@pm-twin/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PM-Twin workflow orchestrator — next actions, transition validation, business process rules",
  "main": "./dist/index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "node esbuild.js",
    "test": "npm run build && node --test tests/**/*.test.js"
  },
  "dependencies": {
    "@pm-twin/collaboration-models": "file:../collaboration-models",
    "@pm-twin/decision-engine": "file:../decision-engine",
    "@pm-twin/lifecycle": "file:../lifecycle"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
