{
  "name": "@pm-twin/commands",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ADR-002 command contracts — zero-dependency, pure TypeScript DTOs only",
  "main": "./dist/index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "index.d.ts"
  ],
  "scripts": {
    "build": "node esbuild.js",
    "type-check": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "~6.0.2"
  }
}
