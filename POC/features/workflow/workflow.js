/**
 * Platform Workflow page – shows the full flow from registration to contract execution.
 * Public page; no auth required.
 */
function initWorkflow() {
    // Links use data-route; global nav handler will handle navigation.
    // No additional setup required.
}
